# Дата: 2026-02-24

  

## Что было сделано

Продолжаю разработку страницы dashboard. 

Сегодня добавил туда отображение Streak в днях. И приступил к разработке истории сессий. Применял всё тот же метод с flex, что и вчера с прогрессом. 

Но в какой-то момент понял, что я не проверил, что будет, если, например, название темы большое, и в итоге оказалось, что всё ломается. Пришлось это чинить. Поставил у названия темы `word-break: break-all;`, изменил `flex` c  `flex: 0  0  25%;` на `flex: 0  0  35%;` и `min-width: 0;`, еще у прогресса изменил `flex` с  `flex: 1;` на `flex: 1  1  auto;` и тоже добавил `min-width: 0;`. В итоге стало отображаться нормально.
 
 Дальше нужно было сделать пагинацию. Благо, что в Async Race была нужна пагинация, и тогда у меня она была написана так, что я мог и здесь её использовать без особо больших изменений. Добавил здесь между кнопками счётчик страниц, внутри функции `nextPrevComponent` создал ещё одну функцию под названием `update`. И вернул из функции `nextPrevComponent` объект такого вида:
```typescript
 { container:  HTMLElement; update: (currentPage:  number, totalPages:  number) =>  void;}
```

Затем создал в классе `DashboardPage` метод `renderSessions`, вот так он выглядит:
```typescript
private  renderSessions() {
	this.sessionsContainer.innerHTML  =  "";
 
	this.totalPages  =  Math.ceil(this.userSessionArr.length  /  this.SESSION_PER_PAGE,);
	
	const  start  = (this.currentPage  -  1) *  this.SESSION_PER_PAGE;
	const  end  =  start  +  this.SESSION_PER_PAGE;

	const  pageSessions  =  this.userSessionArr.slice(start, end);

	for (const  session  of  pageSessions) {
		this.sessionsContainer.append(sessionComponent(session));
	}

	this.pagination?.update(this.currentPage, this.totalPages);
}
 ```
 
Вызываю его в конце метода `create` и каждый раз при изменении страниц.

Потом немного изменил кнопку перехода к library, которую добавил ещё когда делал роутинг. 
Ну и в приципе на этом можно считать, что моя страница готова, всё, что было в примере я реализовал, но может команда ещё чего-нибудь посоветует добавить.

Пока что не пушил на GitHub, потому что мой [Pull Request](https://github.com/Nastya-Student/rss-fe-final-project/pull/4) получил одобрение от команды, как раз в тот момент, когда я доделывал кнопку. Я замерджил его в `develop`. Но возникла небольшая проблема ...

Также созванивался сегодня с Ульной, у неё было несколько вопросов. Надеюсь, что смог ей помочь.

## Проблемы

Так как я хотел продолжать работать над своей страницей, пока команда ещё не проверила моковые данные, то я еще раз уже в своей ветке создал интерфейсы `User` и `TopicProgress`. И в коде создания страницы создал массивы с данными этих интерфейсов. Но так как в процессе работы я понял, что хотелось бы, чтобы в интерфейсе `TopicProgress` было ещё и название темы, то я добавил это поле в интерфейс. И потом, когда я доделал кнопку, замерджил моковые данные в `develop` и хотел подтянуть изменения в свою ветку, то возник мердж конфликт. 

  
## Решения (или попытки)

Благо, что на нашем командном созвоне я решал мердж конфликт по чутким руководством моего ментора Александра. Но решать конфликт по чутким руководством и самостоятельно это разные вещи. Сначала не особо получилось, и в какой-то момент, мне показалось, что я удалил все то, что сделал на своей странице, но в итоге разобрался, и получилось решить конфликт. 


## Мысли / Планы

Так как у меня в методе `create` в классе `DashboardPage` остались массивы с данными, которых там быть не должно, я не стал пока что пушить. Я думал, что надо бы создать сервисы и контроллеры, чтобы получать пока что моковые данные из JSON файлов, но создать эти методы нужно правильно, чтобы потом, если сделаем бэк, не пришлось особо ничего изменять. И как раз в чате команды Настя предложила создать эти методы, я сначала подумал, что она хочет этим заняться, но в итоге решили, что я займусь. 
  

### Затраченное время: 6 часов