# Дата: 2026-03-02

  

## Что было сделано

Для начала работы с [Widget Engine](https://github.com/rolling-scopes-school/tasks/blob/master/stage2/tasks/rs-tandem/examples/01-widget-trainer/widget-engine.md) сделал шаблоны для создания всех виджетов, может ещё туда надо было что-нибудь добавить, но пока они выглядят примерно вот так:
```typescript
export  default  function  quizWidget():  HTMLElement {
  const  quizWidgetContainer  =  new  ElementCreator({classes: ["quiz-widget-container"],}).getElement();

  new  HeadingsCreator(HEADINGS_TWO, {
    parent:  quizWidgetContainer,
    text:  "Quiz Widget",
  }).getElement();
  
  return  quizWidgetContainer;
}
```

Сделал [Pull Request #23](https://github.com/Nastya-Student/rss-fe-final-project/pull/23).

Но потом посмотрел, что когда создавал интерфейсы и моковые данные для виджетов, то сами виджеты очень отличаются друг от друга, и решил, что нужно это исправить. Теперь интерфейсы виджетов отличаются только полем payload, думаю, что это правильнее.

Только если раньше type guards были попроще, потому что проверялся только на соответствие базовому виджету, то теперь я их расширил для каджого payload. 

На самом деле хорошо, что полез исправлять, потому что когда изменил интерфейсы, полезли ошибки в `widget.service.ts`, и оказалось, что когда я делал сервисы и копировал код, и изменял под нужный вид, то забыл поменять endpoint, в итоге у нас в `WidgetService` был endpoint `users`, но теперь я это исправил.

Надеюсь, что в этот раз ничего не просмотрел. 

Сделал [Pull Request #26](https://github.com/Nastya-Student/rss-fe-final-project/pull/26).  

## Мысли / Планы

Так как я сделал [Pull Request #23](https://github.com/Nastya-Student/rss-fe-final-project/pull/23) перед тем, как полез исправлять интерфейсы и данные, то после того, как команда одобрит [Pull Request #26](https://github.com/Nastya-Student/rss-fe-final-project/pull/26), хочу исправить немного шаблоны виджетов.

### Затраченное время: 3,5 часа