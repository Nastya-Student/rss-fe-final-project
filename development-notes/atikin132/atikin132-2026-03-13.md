# Дата: 2026-03-13


## Что было сделано
Сегодня проверял Pull Requests. Есть [Pull Request #8](https://github.com/Nastya-Student/rss-fe-final-project/pull/8), который проверял до этого и он уже закрыт, поэтому включу его тоже в пункт Code Review.


## Code Review

- [PR #8: Feat/RST-10 add landing page](https://github.com/Nastya-Student/rss-fe-final-project/pull/8) — 14 комментариев (предложил убрать комментарии в коде; добавить util для создания `section`; вынести магические числа в константы; добавить кнопкам hover)
- [PR #29: feat/RST - 11 implement authorization page](https://github.com/Nastya-Student/rss-fe-final-project/pull/29) — 11 комментариев (предложил заменить абсолютные значения в css на относительные; предложил вынести строки в константы; предложил называть css с привязкой к конкретной странице, чтобы не произошло наложение стилей; предложил объединить повторяющиеся css свойства, для соблюдения принципа DRY; предложил убрать `!important;`; предложил перенести вспомогательные рисунки в css, а svg код картинки перенести в отдельные файлы, чтобы отказаться от использования `innerHTML`; предложил использовать готовые утилиты для создания элементов, вместо написанной функции  `createElement` c 4 блоками `if`, из-за которой нарушается принцип DRY, потому что утилиты для создания элементов уже есть в `develop`; предложил убрать `min-height:  100vh;` из-за которого появляется ненужный вертикальный скролл )
- [PR #28: Feat/RST-22 add header](https://github.com/Nastya-Student/rss-fe-final-project/pull/28) — 11 комментариев (предложил убрать комментарии в коде; предложил вместо добавления полей в интерфейс `CreateAnchorOptions` унаследовать его от `CreateElementOptions`;  предложил перенести вспомогательные рисунки в css, а svg код картинки перенести в отдельные файлы, чтобы отказаться от использования `innerHTML`; предложил вызвать `header.getElement()` один раз и записать в переменную, а не вызывать `header.getElement()` несколько раз; спросил зачем задавать элементу `display: flex;` в TS коде, если уже задан в css классе; предложил давать конкретные названия для переменных; предложил при нажатии на логотип вместо `target="_blank"` использовать `target="_self"`   )

## Мысли / Планы

Начать разбираться, как писать тесты.
  

### Затраченное время: 1 час