# Дата: 2026-03-07

  
  

## Что было сделано

  
Сегодня внёс изменения в dashboard,  которые предложила Ульяна. Там были моменты с семантикой, поменял цвет и ширину прогресс бара у Total Progress, и добавил контейнер, чтобы элементы внутри страницы dashboard сильно не растягивались. Теперь мой [Pull Request #17](https://github.com/Nastya-Student/rss-fe-final-project/pull/17) ждет проверки от ещё хотя бы одного участника команды.

Затем переключился на Widget Engine. В том коде, что был готов, не было нормального перехода из библиотеки к виджетам, чтобы это исправить, я подумал, что можно слушать событие window - hashchange, и в замисимости от того, какая тема выбрана и какой URL, показывать виджеты по этой теме, правда оно слушает любые переходы по страницам и это немного напрягает.

После этого пренес `renderResultsScreen()` в отдельный файл и вынес в папку компонентов, и в Widget Engine просто вызываю функцию рендеринга результатов, которая пока что показывает название и кнопку возврата в библиотеку.

Затем подчищал немного код `WidgetEngine`, убирал лишнее (например, были лишние параграфы), переводил всё на константы.

После этого захотел, чтобы шаблоны виджетов отображались как карточки в landing и в dashboard. Ещё добавил чтобы цвет блока, где написано "Correct" или "Wrong" менялся в зависимости от значения, зеленый для "Correct" и красный для "Wrong". Также добавил, чтобы кнопка "Submit" блокировалась после первого нажатия.

Ну и в целом сегодня приводил Widget Engine в нормальный вид, поэтому особых проблем не возникло. Может быть команда что-нибудь найдет и я потом поправлю. Но пока сделал [Pull Request #27](https://github.com/Nastya-Student/rss-fe-final-project/pull/27) и буду ждать ревью.


Затем записал и выложил на YouTube [видео с демонстрацией страницы dashboard](https://youtu.be/BDhY2dRk-Lk). Получилось с 3 попытки, до этого выходили видео по 20 минут. В первый раз рассказал еще и про Widget Engine. Во второй раз уже рассказывал только про dashboard, но видимо что-то сильно долго объяснял. И уже в третий раз уложился в 10 минут.

  


## Мысли / Планы

Если страницу library ещё не начали делать, то взял бы её себе. Или можно взять страницу результатов. Ну или уже буду брать какой-нибудь виджет.
  

  

### Затраченное время: 4 часа