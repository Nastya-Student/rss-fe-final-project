# Дата: 2026-03-22

## Что было сделано
Сегодня занимался интеграцией Supabase в приложение и переходом с моковых данных на реальные. 
Настроил таблицу profiles, добавил RLS политики и триггер для автоматического создания профиля при регистрации пользователя. 
Разобрался с авторизацией через Google и тем, как правильно обрабатывать редирект и инициализацию сессии после логина.
Переписал user.service под Supabase с нормальной типизацией, убрал any и привёл код к требованиям ESLint.
Также обновил dashboard: теперь данные пользователя, прогресс и сессии подтягиваются из базы через user.id, а не через захардкоженный USER_ID.

## Проблемы
Supabase местами возвращает плохо типизированные данные, из-за чего много возни с unknown и any.
По итогу с помощью нейросети пытаюсь привести код чтобы убрать ошибки ESlint, на выходе получаю совершенно другой код
30 минут на код, 120 минут исправить чтобы TS и ESlint не было ошибок
406 ошибка из-за отсутствия записи в profiles


## Решения (или попытки)
Проблему с 406 решил — оказалось, что таблица profiles была пустая, добавил пользователя вручную / через триггер.